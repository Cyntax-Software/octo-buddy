export const config = {
  // how long to wait for response from the API
  apiRequestTimeout: 500,

  // the max number of IP addresses to search when
  // looking for available octoprint servers
  serverSearchMaxIp: 30,
};
